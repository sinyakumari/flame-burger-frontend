import React from "react";

const NotFoundPage = () => {
  return (
    <div
      style={{
        height: "100vh",
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        fontSize: "28px",
        fontWeight: "bold"
      }}
    >
      404 Page Not Found
    </div>
  );
};

export default NotFoundPage;